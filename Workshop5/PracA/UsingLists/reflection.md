Task1:
C# compares the strings and places them in ascending order based on their default string comparison. For normal names, this appears as alphabetical order from A to Z.

Task2:
Arrays use '[]' and have a fixed size. Lists use 'List<T>' and can add another range of array converting into list or can shrink.

Arrays and Lists both store multiple values.

Arrays are good when the size is known. Lists are better when we need to add or remove items often.
